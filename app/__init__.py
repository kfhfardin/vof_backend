"""Voice of the Field — backend service.

The FastAPI app, Orchestrator engine, and arq workers all live under this package.
See `app/main.py` for the ASGI entry point and `app/factory.py` for the app factory.
"""

__version__ = "0.1.0"
